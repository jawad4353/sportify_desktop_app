enum FilterCategory {
  sort,
  sports,
  ratings,
  priceRange,
  nearby,
}

enum SortOption {
  recommended,
  ratingHighToLow,
  priceLowToHigh,
  priceHighToLow,
}

enum BookingFilterCategory {
  status,
  name,
  date,
}

enum BookingStatusFilter {
  all,
  upcoming,
  completed,
  cancelled,
  pending,
}

enum BookingDateFilter {
  all,
  today,
  thisWeek,
  thisMonth,
  custom,
}
